# =======================
# Required global vars
# =======================
# Your base domain (no protocol)
DOMAIN=example.com
# Email for Let's Encrypt
LETSENCRYPT_EMAIL=you@example.com

# =======================
# Google Drive remote
# Used by the rclone container
# Make sure you have configured this remote locally
RCLONE_REMOTE=mygdrive:
# Local mount point inside the rclone container
RCLONE_MOUNT_PATH=/media/gdrive

# =======================
# Credentials & passwords
# =======================
# NPM (Nginx Proxy Manager) 
NPM_DB_USER=npm
NPM_DB_PASS=changeme
# Qbittorrent WebUI
QBT_USER=admin
QBT_PASS=adminadmin
# Plex claim token (optional; for initial setup)
PLEX_CLAIM=
# Prowlarr API key (will be auto-generated on first run if blank)
PROWLARR_API_KEY=
# Radarr API key (same)
RADARR_API_KEY=
# Sonarr API key (same)
SONARR_API_KEY=
